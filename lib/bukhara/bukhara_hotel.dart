import 'package:flutter/material.dart';
class BukharaHotel extends StatelessWidget {
  const BukharaHotel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Hotels in Bukhara')),
    );
  }
}
